-- =============================================================================
-- 목적
--   1단계 배포에서 "선택 가능한 구"를 6개로 맞추는 방법을 정리한다.
--
-- 앱 동작 (권장)
--   gourmet `.env` 에 DEPLOY_STAGE1=true 를 두면 API 가 허용 구만 노출한다.
--   broke 는 기본 빌드(VITE_BROG_FULL_MAP 미설정)에서 UI 도 동일 6구를 쓴다.
--   코드 상 기본 6구: 마포·용산·서대문·영등포·종로·중 (deploy_stage1.py / deployStage1.ts).
--
-- DB 보조 (선택)
--   API 필터만으로도 충분하다. 관리 도구에서 "비활성 구"가 보이면 혼동을 줄이기 위해
--   아래처럼 나머지 구의 active 를 false 로 둘 수 있다. 운영 전 백업 후 실행할 것.
-- =============================================================================

-- 예시 (시드에 강남·송파·성동 등이 있을 때만 의미 있음). 주석 해제 후 실행.
-- UPDATE districts SET active = false
-- WHERE name NOT IN (
--   '마포구', '용산구', '서대문구', '영등포구', '종로구', '중구'
-- );

-- 2단계(전 구) 전환 시 DEPLOY_STAGE1 를 끄고, 필요하면 active 를 다시 켠다.
-- UPDATE districts SET active = true;
