# broke — npm run build / vite build 시 주입 (이 파일을 .env.production 으로 복사 후 값 수정)
# www 는 nginx 정적, API 는 api 서브도메인( deploy/nginx/www-api-2corea.conf 와 동일 전제)

VITE_API_BASE_URL=https://api.2corea.com
# VITE_KAKAO_MAP_APP_KEY=
# VITE_KAKAO_REST_API_KEY=
